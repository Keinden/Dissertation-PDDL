(define (problem dockyard_fsm_problem) (:domain dockyard)
    (:objects
        agent1 agent2 - agent
        truck1 - truck
        ship1 - ship
        
    )

    (:init
        
    )

    (:goal (and
        
    ))
)
